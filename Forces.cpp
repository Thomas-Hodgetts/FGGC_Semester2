#include "Forces.h"


Vector3D* Forces::Update(Vector3D* netforce)
{

	return netforce;
}
Vector3D* Forces::Update(Vector3D* netforce, float Mass, float OpposingMass, Vector3D* Distance)
{

	return netforce;
}